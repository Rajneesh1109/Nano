# Smart Time Tracker (Web) - Example Environment Variables
# Copy this file to `.env` and fill in your real values.

# Supabase Project URL (Supabase Dashboard -> Project Settings -> API)
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co

# Supabase Anon Public Key (Supabase Dashboard -> Project Settings -> API)
# This is safe to use in the browser, but still treat it as a credential (don't commit real values).
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_PUBLIC_KEY

# API URL for the backend server
VITE_API_URL=https://nono-ral1.onrender.com
